---
# First-party composite refs deliberately track @main so one library commit
# ships matching workflows and actions; everything third-party stays SHA-pinned.
version: 3
ignore_actions:
  - name: centrifuge/github-actions-lib/.*
    ref: main
